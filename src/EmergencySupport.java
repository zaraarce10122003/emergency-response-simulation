public interface EmergencySupport {

    // deploys whatever help this unit has
    void deployAid();

    // returns a report as a STRING (important for the grader)
    String getEmergencyReadinessReport();
}

